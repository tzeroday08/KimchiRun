using UnityEngine;

public class GameManager : MonoBehaviour
{
    int stageNumber = 0;
    int totalScore = 0;
    void Start()
    {
        Debug.Log(stageNumber);
        Debug.Log(totalScore);
    }

    void Update()
    {
        
    }
}
